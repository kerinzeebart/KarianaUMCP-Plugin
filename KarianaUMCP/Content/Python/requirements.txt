# KarianaUMCP Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
pillow>=9.0.0          # Screenshot compression and image processing
fastmcp>=2.13.0        # FastMCP 2.0 for MCP server framework

# Optional dependencies (enhanced features)
pyngrok>=5.0.0         # ngrok tunnel for remote access
gradio>=4.0.0          # Web dashboard UI

# Development dependencies (for testing)
pytest>=7.0.0          # Unit testing
pytest-asyncio>=0.21.0 # Async test support
